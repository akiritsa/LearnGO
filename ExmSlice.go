package main

import (
	"fmt"
	"error"
)

func main() {
	var messages []string
	messages[0] = "1"
	11
	fmt.Println(messages)
	12
	// messages := make([lstring)
	14
	// printMessages (messages)
}

func printMessages (messages [] string) error{
		if len (messages) == 0{
		return errors.New("empty array")
	}
		messages[0] = "5"
		fmt.Println(messages)
		return nil
	}
